# MIN_ROM.cfg - Linker konfigurace pro minimalni ROM build
# Mapovani: ZP $0000, RAM $0200, ROM $E000, JMPTBL $FF00, VECTORS $FFFA

MEMORY
{
    ZP:  start = $0000, size = $0100, type = rw, define = yes;
    RAM: start = $0200, size = $5E00, type = rw, define = yes;
    ROM: start = $E000, size = $2000, fill = yes, fillval = $FF;
}

SEGMENTS
{
    ZEROPAGE: load = ZP,  type = zp,  define   = yes;
    DATA:     load = ROM, type = rw,  define   = yes, run = RAM, offset = $200;
    BSS:      load = RAM, type = bss, define   = yes;
    HEAP:     load = RAM, type = bss, optional = yes;
    STARTUP:  load = ROM, type = ro;
    ONCE:     load = ROM, type = ro,  optional = yes;
    CODE:     load = ROM, type = ro;
    RODATA:   load = ROM, type = ro;
    JMPTBL:   load = ROM, type = ro,  start    = $FF00;
    VECTORS:  load = ROM, type = ro,  start    = $FFFA;
}

FEATURES
{
    CONDES: segment = STARTUP,
            type    = constructor,
            label   = __CONSTRUCTOR_TABLE__,
            count   = __CONSTRUCTOR_COUNT__;
    CONDES: segment = STARTUP,
            type    = destructor,
            label   = __DESTRUCTOR_TABLE__,
            count   = __DESTRUCTOR_COUNT__;
}

SYMBOLS
{
    __STACKSIZE__:  value = $0200, type = weak;  # velikost cc65 softwaroveho zasobniku
    __STACKSTART__: value = $5FFF, type = weak;  # vrchol RAM oblasti ($0200-$5FFF), zasobnik roste dolu
}
